name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    name: Swift package tests
    runs-on: macos-26

    steps:
      - uses: actions/checkout@v6

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build

      - name: Test
        run: swift test
